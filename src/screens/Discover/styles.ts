import {StyleSheet} from 'react-native';
import {FontFamilies, ThemeColors} from '../../Constants';
export const DiscoverScreenStyle = StyleSheet.create({
  DiscoverScreenContainer: {
    backgroundColor: '#fff',
  },
});
